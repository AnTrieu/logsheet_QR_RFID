<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Bo góc -->
    <corners android:radius="15dp" />

    <!-- Màu nền -->
    <solid android:color="#e9ecef" />

    <!-- Đường viền -->
    <stroke
        android:width="1dp"
        android:color="#848C94" />
</shape>
